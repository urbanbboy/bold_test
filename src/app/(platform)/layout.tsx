

const PlatformLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <div>
            Platform layout
            {children}
        </div>
    );
}

export default PlatformLayout;