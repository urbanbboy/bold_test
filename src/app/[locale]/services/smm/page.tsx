import SmmPage from "@/domains/services/smm"
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Digital продвижение",
};

const Smm = () => {
    return <SmmPage />
}

export default Smm